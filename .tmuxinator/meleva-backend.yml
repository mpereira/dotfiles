# ~/.tmuxinator/meleva-backend.yml

project_name: meleva-backend
project_root: ~/meleva/backend
tabs:
  - shell: git remote update
  - server:
      layout: main-vertical
      panes:
        - rake shell
        - rake server
  - editor: vim
