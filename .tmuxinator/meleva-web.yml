# ~/.tmuxinator/meleva-web.yml

project_name: meleva-web
project_root: ~/meleva/web
tabs:
  - shell: git remote update
  - watcher:
      layout: main-vertical
      panes:
        - make watch
        - make server
  - editor: vim
